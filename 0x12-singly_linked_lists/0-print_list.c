#include "lists.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * print_list - prints all elements of list
 * @h: list
 * Return: num of nodes, [0] (nil) if str NULL
 */

size_t print_list(const list_t *h)
{
}

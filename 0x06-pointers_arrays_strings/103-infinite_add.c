#include "main.h"

/**
 * infinite_add - adds two numbers
 * @n1: first number
 * @n2: second number
 * @r: buffer that function will use to restore result
 * @size_r: buffer size
 * Return: pointer to result, 0 if result not stored in r
 */

char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	return (r);
}

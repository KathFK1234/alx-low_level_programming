#include "main.h"

/**
 * _strlen - returns lenth of string
 * @*s: character to return length of
 * Return: Length of string
 */

int _strlen(char *s)
{
	len = strlen(*s);

	_putchar('%d\n', len);
}

#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0
 */

int main(void)
{
	unsigned long w = 2;
	unsigned long y = 612852475143;

	while (y > w)
	{
		if (y % w == 0)
		{
			y = y / a;
		}
		else
			w++;
	}
	printf("%lu\n", y);
	return (0);
}

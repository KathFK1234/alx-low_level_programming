#include "main.h"

/**
 * create_array - creates array of chars initd by spec char
 * @size: size of array
 * @c: character
 * Return: pointer to array, NULL if fail or size = 0
 */

char *create_array(unsigned int size, char c)
{

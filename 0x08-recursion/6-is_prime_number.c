#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * is_prime_number - checks for prime number
 * @n: integer to check
 * Return: 1 if prime, 0 if else
 */

int is_prime_number(int n)
{
}

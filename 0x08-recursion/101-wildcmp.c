#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * wildcmp - compares two strings
 * @s1: first string
 * @s2: second string
 * Return: 1 if identical, 0 if else
 */

int wildcmp(char *s1, char *s2)
{
}

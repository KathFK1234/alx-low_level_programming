#include "main.h"

/**
 * main - compute absolute value of integer
 * _abs - compute absolute value of integer
 * @int: integer
 * Return: Always 0
 */

int _abs(int)
{
	int =  n;

	if (n > 0)
		return (n);
	else if (n < 0)
		return (-n);

	return (0);
}

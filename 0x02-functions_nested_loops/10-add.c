#include "main.h"
#include <stdio.h>

/**
 * add - add two integers
 * @a: first integer
 * @b: second integer
 * Return: sum of integers
 */

int add(int a, int b)
{
	int result = a + b;
	return (result);
}
